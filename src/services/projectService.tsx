import { Project } from "../types/Project.tsx";

export function updateProjectStatus(projectId: any, newStatus: any) {
    //something here
    alert('Updating status to ' + newStatus + ' for project ' + projectId)
}